#!/bin/sh

# Unix
alias ll="ls -lha"
alias flushdns='dscacheutil -flushcache'
alias tmuxcopy='tmux show-buffer | pbcopy'
alias e='emacsclient --no-wait'

# Bundler
alias be="bundle exec"
alias bi="bundle install"

# gem
alias gemi='gem install --no-rdoc --no-ri'

# git
alias gl='git log'
alias gs='git status'
alias gsp='git smart-pull'
alias gd='git diff'
alias gdc='git diff --cached'
alias gap='git add -p'
alias gc='git commit'

# Code highlighting
alias highlight_pastebuffer='pbpaste | highlight --syntax=ruby --style=solarized-dark --out-format=rtf | pbcopy'

# Alias git to hub
alias git=hub

# vim ft=sh
